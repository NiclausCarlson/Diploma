import Diploma.Computational
import Diploma.Polynomials.Polynomials

namespace algebra
  open polynomials
  open computational
  
  -- class MonomialRing (α: Type) [ToString α] [Inhabited α] [Computational α] :=
  --   (zero: Polynomial α)
  --   (one: Polynomial α)
  --   (sum: Polynomial α → Polynomial α → Polynomial α)
  --   (sum_zero: (∀ p: Polynomial α, sum p zero = p))
  --   (sum_assoc: (∀ p q: Polynomial α, sum (order p) (order q) = sum (order q) (order p)))
  --   (sum_comm: (∀ m p q: Polynomial α, sum (sum (order m) (order p)) (order q) = 
  --                                      sum (order m) (sum (order p) (order q))))
  --   (mul: Polynomial α → Polynomial α → Polynomial α)
  --   (mul_one: (∀ p: Polynomial α, mul p one = p))
  --   (mul_assoc: (∀ m p q: Polynomial α, mul (mul (order m) (order p)) (order q) = 
  --                                      mul (order m) (mul (order p) (order q))))
  --   (left_dist: (∀ m p q : Polynomial α, mul m (sum p q) = sum (mul m p) (mul m q)))
  --   (right_dist: (∀ m p q : Polynomial α, mul (sum p q) m = sum (mul p m) (mul q m)))

  

end algebra
